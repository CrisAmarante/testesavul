rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras padrão - ajuste conforme sua necessidade
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
